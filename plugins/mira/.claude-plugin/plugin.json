{
  "name": "mira",
  "description": "Bridges chat from the Mira iOS app into Claude Code",
  "mcpServers": {
    "mira": {
      "command": "sh",
      "args": [
        "-lc",
        "exec bun \"${CLAUDE_PLUGIN_ROOT}/server.ts\" 2>>/tmp/mira.log"
      ]
    }
  }
}
